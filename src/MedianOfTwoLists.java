 class MedianOfTwoLists {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        int len = nums1.length + nums2.length - 1;
        int [] arr = new int[len];
        // merge the two lists based
    }
}

